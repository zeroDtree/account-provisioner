[project]
name = "account-provisioner"
version = "0.1.0"
description = "Poll upstream API provision/pending and run isolation scripts"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
    "ty>=0.0.35",
]
